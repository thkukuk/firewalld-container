# Variables of this file can be overwritten with
# /etc/default/container-firewalld

# Name of the image path to pull the firewalld image from
FIREWALLD_IMAGE_PATH=registry.opensuse.org/home/kukuk/container/firewalld:latest

# Name of the path where the configuration will be stored
CONFIG_DIR="/srv/firewalld"

